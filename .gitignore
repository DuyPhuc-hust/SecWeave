# Tài liệu nguồn dạng export (nặng, binary-like) — giữ local, không đưa vào git history.
# Nếu muốn version control cả hai file này, xoá 2 dòng dưới và git add -f.
A.html
SecWeave.pdf

__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.db
.secweave/
.secweave_agent_bridge/
.env
.DS_Store
