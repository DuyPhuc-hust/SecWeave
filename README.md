# SecWeave

Feasibility pilot nội bộ (CSP) để kiểm tra: có thể vận hành một quy trình xác minh security finding **có phép, có bằng chứng độc lập, có kết luận tất định và có ngữ cảnh tái sử dụng** hay không.

Kiến trúc bốn tầng tách quyền: **Hypothesis Engine** → **Exploit Agent** → **Evidence Harness & Store** → **Deterministic Verdict Oracle**, với Human Review và Security Context Store bao quanh. Nguyên tắc cốt lõi: *bên tạo ra giả thuyết không được tự chấm bài của mình*.

## Tài liệu

| File | Nội dung |
|---|---|
| [`SECWEAVE_SPEC.md`](./SECWEAVE_SPEC.md) | Đặc tả kiến trúc kỹ thuật: 4 tầng, mô hình dữ liệu, Verification Package, kiểm soát & an toàn |
| [`SECWEAVE_WEEKLY_PLAN.md`](./SECWEAVE_WEEKLY_PLAN.md) | Kế hoạch triển khai theo tuần (W1–W8), chiến lược test theo từng tầng |
| [`TARGET_SANDBOX_AUTHORIZATION_TEMPLATE.md`](./TARGET_SANDBOX_AUTHORIZATION_TEMPLATE.md) | Biểu mẫu Gate 2 (chưa điền tên/ký) — sản phẩm Chặng 1 theo weekly plan W2 |

## Trạng thái

Giai đoạn **Chặng 1 — Discovery/preparation**. Chưa có active run trên target nào. Xem điều kiện Gate 0–5 trong `SECWEAVE_SPEC.md` §6.

Đã chạy được thật (xem [Dùng CLI](#dùng-cli) dưới đây): Signal Normalizer (Semgrep/Trivy/OWASP ZAP), Hypothesis Engine, Exploit Agent (lập kế hoạch + tự kiểm duyệt qua allowlist — **chưa thực thi hành động thật**), Security Context Store. Evidence Harness (`evidence_harness/harness.py`) đã có bản lõi + blind marker + **identity/session thật theo từng identity**: mỗi identity có 1 `httpx.Client`/cookie-jar riêng, `login()` xử lý được cả session theo cookie lẫn bearer-token-trong-body (JWT — xác nhận thật với OWASP Juice Shop, `/rest/user/login` trả token trong JSON body chứ không phải cookie), tổng quát cho mọi target chứ không hard-code riêng cho web nào. Nhờ đó `positive_control`/`denied_control` giờ test được đúng nghĩa (2 tài khoản thật, cùng 1 resource) — đã dùng chính cơ chế này phát hiện lại đúng lỗ hổng IDOR thật của Juice Shop (tài khoản B đọc được basket của tài khoản A). Request/response headers (kể cả cookie/token) được redact trước khi ghi ra đĩa. Policy Service (`shared/policy.py`) kiểm cả `action.parameters` lẫn query string trong `target` (không chỉ path) — cú pháp allowlist `"METHOD url [params:key1,key2=regex]"`. Mỗi key allowlist giờ có thể kèm `=regex` để bắt buộc kiểm cả **giá trị** tham số, không chỉ tên (2026-08-18 — đóng gap đã biết từ trước: tên tham số nằm trong allowlist không có nghĩa MỌI giá trị của nó đều an toàn, vd `userId` được phép nhưng giá trị gửi đi lại là identity khác hoặc payload injection). Key không kèm `=regex` vẫn nhận mọi giá trị như cũ — không phá allowlist string đã có từ trước. Review độc lập ngay sau đó tìm thêm 2 lỗi thật: dấu phẩy trong `{m,n}` (cách viết "N chữ số" tự nhiên nhất) bị cắt nhầm thành 1 pattern cụt + 1 key rác được phép mọi giá trị — sửa bằng cách chỉ tách dấu phẩy ở top-level (không nằm trong `{}/()/[]`); `str(None) == "None"` có thể khớp pattern kiểu chữ cái — sửa bằng luôn từ chối giá trị `null` khi key đó có khai báo pattern, bất kể pattern gì. Cả 2 đã sửa + có test + mutation-verified.

**Verdict Oracle** (`verdict_oracle/oracle.py`+`predicates.py`) có predicate draft **và** bước gộp verdict cuối (`assemble_verdict`/`decide` — trả đúng 1 trong 3 `CONFIRMED`/`NOT_REPRODUCED`/`INCONCLUSIVE`), giờ enforce đủ 2 control SPEC không-ngoại-lệ tìm thấy còn thiếu qua audit toàn dự án (2026-08-16): (1) `execution_status` bắt buộc phải `COMPLETED` mới ra verdict cuối — STOPPED/RUNNING/... đều ép về INCONCLUSIVE (SPEC §3.4); (2) hash artifact được xác minh lại (đọc file thật, tính lại SHA-256) trước khi cho phép CONFIRMED — hash không khớp hoặc file không đọc được đều thành INSUFFICIENT_DATA (SPEC §6.4 control #8). Review thủ công 2026-08-17 (2 Agent review bị fail do hết session limit nên tự đọc code trực tiếp) tìm thêm 2 lỗi: hàm hash-check crash thay vì trả INSUFFICIENT_DATA nếu `raw_evidence_ref` chứa NUL byte (chưa khai thác được qua pipeline hiện tại, nhưng cùng dạng lỗi "except quá hẹp" đã gặp 2 lần trước); `VerdictResult` là model "quyết định an toàn" duy nhất chưa có validator tự kiểm tra tính nhất quán như 6 model khác cùng loại — giờ đã enforce `verdict=confirmed` bắt buộc cả 3 nhóm predicate đều `satisfied`.

**Kill-switch** (`shared/kill_switch.py`, SPEC §6.3/§5.3, weekly plan Tuần 6) đã có: `KillSwitch` là điểm dừng dùng chung cho 1 execution — 6 nguồn (`StopSource`: operator/target owner/infra owner/data-ISMS owner/incident responder/automatic threshold), gọi `stop()` từ bất kỳ nguồn nào cũng dừng ngay, không cần thương lượng trước, chạy đúng 1 lần cleanup (callable operator tự cung cấp — chưa có Gate 3 thật), ghi audit log JSONL (ai/khi nào/vì sao) tại `{storage_dir}/{execution_id}/kill_switch_audit_log.jsonl`. `resume()` là đường DUY NHẤT quay lại RUNNING — không có auto-resume/timer nào (đối chiếu với circuit-breaker pattern để tránh nhầm 2 khái niệm). `EvidenceHarness.capture()` nhận `kill_switch` optional, raise `ExecutionStoppedError` thay vì gửi request thật khi đã STOPPED — đây là kiểm tra trước-khi-gửi (cooperative), không ngắt ngang request đang bay giữa chừng. Đã qua 2 vòng review độc lập, cả 2 vòng đều tìm ra bug thật trong chính bản fix của vòng trước (status không phục hồi đúng khi tạo lại instance sau STOPPED; write audit log ngoài lock làm sai thứ tự khi so bằng physical file order — sửa bằng `sequence` số nguyên gán tại thời điểm chuyển trạng thái, không phụ thuộc lúc I/O thật sự chạy xong; log hỏng dòng cuối do crash giữa chừng làm `__init__` crash theo — sửa bằng fail-safe về STOPPED). `StopEvent` giờ có field `automatic_threshold_reason` cấu trúc (1 trong 5 điều kiện tự động ở SPEC §6.3) thay vì chỉ nhét vào `reason` dạng free-text — bắt buộc khi `source=automatic_threshold`, cấm khi không phải. Chưa có: nối kill-switch với CLI/API thật (xem hạn chế 2 lớp mô tả bên dưới).

**Cost Service thật** (`shared/cost.py::CostService`, SPEC P6/§6.4 control #9, weekly plan Tuần 6) đã có: đếm hành động THỰC TẾ đã thực thi qua `EvidenceHarness.capture()` (không chỉ dự kiến), lưu bền `cost_audit_log.jsonl` để sống sót qua restart, tự động gọi `kill_switch.stop(source=automatic_threshold, automatic_threshold_reason=action_count_exceeded)` và từ chối hành động sẽ vượt cap TRƯỚC khi gửi, không phải sau. Qua 1 vòng review độc lập ngay sau khi build (không đợi ai hỏi), tìm ra 4 lỗi thật: `KillSwitch.stop()` validate field mới quá trễ (sau khi đã flip status + chạy cleanup thật — sửa bằng validate ngay đầu hàm, trước mọi side effect); dòng log cuối bị crash dở dang (torn line) rồi bị merge với entry hợp lệ ghi sau đó, làm mất vĩnh viễn 1 hành động khỏi count qua lần restart thứ 2 (sửa bằng đảm bảo luôn xuống dòng trước khi ghi thêm); count trong bộ nhớ tăng trước khi ghi bền xuống đĩa, lệch vĩnh viễn nếu ghi lỗi (sửa bằng ghi trước, tăng count sau); cost check chạy trước `client.build_request()` nên 1 lỗi nội bộ của harness (không liên quan gì đến target) vẫn tiêu tốn 1 slot cost mà không có bằng chứng nào (sửa bằng dời check ra sau khi build_request() thành công). Cả 4 đã sửa, 2 lỗi rủi ro cao nhất có mutation-test, tổng 398 test pass.

**Kill-switch/Cost Service/Evidence Harness nối CLI thật** (2026-08-17) — cả 2 lớp hạn chế nêu trên đã giải quyết: `KillSwitch.refresh()` (đọc lại audit log, nhận trạng thái mới hơn qua so `sequence`, không bao giờ lùi trạng thái) được gọi trong `EvidenceHarness.capture()` trước mỗi request thật, nên `secweave kill` chạy ở process/terminal KHÁC giờ thực sự dừng được `secweave execute` đang chạy, không chỉ ghi thêm dòng log. 3 lệnh CLI mới: `execute` (thực thi plan đã approve, capture role=main cho mọi action — CHƯA tự dựng được kịch bản 3-role vì `ActionSpec` chưa có field đánh dấu role, vẫn cần script tay như trước), `kill`, `resume` (đường duy nhất quay lại RUNNING sau khi bị kill, khớp SPEC §6.4 control #10). Review ngay sau khi build tìm ra 5 lỗi: `execute` crash khi tái dùng `--execution-id` đã RUNNING/STOPPED (giờ tự nhận diện đúng cả 3 trạng thái); `sequence` là counter riêng từng instance chứ không toàn cục, 2 instance đua nhau có thể tính trùng — giờ fail-safe về STOPPED khi phát hiện trùng; `kill` báo "thành công" y hệt dù `--execution-id` gõ sai/chưa từng chạy — giờ cảnh báo rõ; except clause của `execute` bỏ sót lỗi ghi audit log thật của CostService/KillSwitch. Cả 5 đã sửa + có test, 2 lỗi nặng nhất có mutation-test.

**Hardening 2026-08-16** (audit toàn dự án tìm ra 9 lỗ hổng thật ở các tầng chưa từng được review kiểu adversarial, tất cả đã sửa + có regression test): Signal Normalizer không còn crash mất trắng cả report khi 1 field container (`results`/`Vulnerabilities`/`site`/`alerts`/...) là `null` thay vì thiếu hẳn; JSON lồng quá sâu không còn crash bằng `RecursionError` không bắt được. Hypothesis Engine: prompt có delimiter mang mã xác thực ngẫu nhiên riêng từng lần gọi (cả cho `source_snippet` lẫn phần đánh dấu từng SIGNAL khi gộp batch qua `--llm-mode agent`) — dữ liệu không đáng tin (source code, response thật) không còn giả mạo được ranh giới dữ liệu/instruction; chế độ batch của agent-bridge giờ yêu cầu response là JSON object có key rõ ràng (không phải mảng theo vị trí) để không bị gán nhầm câu trả lời sang signal khác. Context Store: lỗi tạo thư mục chứa DB và lỗi đọc dữ liệu (trước đây không bắt exception nào) giờ đều thành lỗi CLI sạch, không còn traceback thô; `show-hypothesis --format json` và `hypothesize --format json` giờ trả về field `location` cùng 1 dạng object lồng nhau, không còn khác nhau.

**Hardening 2026-08-17** (review lần đầu tiên soi toàn bộ lõi `evidence_harness/harness.py` — trước đó chỉ review riêng phần identity và phần kill-switch — tìm ra 8 lỗ hổng thật, rồi review lần 2 xác minh chính các bản fix đó tìm ra thêm 5 lỗi nữa, tất cả đã sửa + có regression test + mutation-test): `login()` không còn để lộ secret thật vĩnh viễn trên đĩa nếu trích token thất bại (giờ xoá toàn bộ response body khi không rõ secret nằm ở đâu), và từ chối token null/rỗng thay vì âm thầm gửi "Bearer None". Transcript giờ ghi đúng URL thật đã gửi (không phải `action.target` nguyên văn — httpx's `params=` có thể thay thế query string sẵn có), và redact được cả secret nằm trong query string của `action.target`. `capture()` giờ raise lỗi rõ ràng thay vì crash khi dùng lại 1 identity sau `close()`. Marker check không còn phân biệt hoa/thường. Response có giới hạn kích thước (10 MiB, tránh OOM) — khi bị cắt bớt, marker không tìm thấy được báo "chưa chắc" thay vì "chắc chắn không có", và phần thân giữ lại được kích thước tối đa thay vì mất trắng. Body không phải UTF-8 được lưu dạng base64 (không mất dữ liệu) thay vì decode lossy im lặng, có đọc charset khai báo trong Content-Type trước khi thử UTF-8.

**Hardening 2026-08-17 (vòng 2)** (review lần đầu soi toàn bộ `exploit_agent/agent.py`, LLM client dùng thật (`openai_compatible_client.py`), `cli.py`, `shared/policy.py` — tất cả trước giờ chỉ được review từng phần nhỏ — tìm ra 7 lỗi thật, review lần 2 xác minh chính các bản fix đó tìm thêm 3 lỗi nữa, cộng với đóng 3 lỗ hổng cũ đã biết nhưng chưa sửa; tổng 13, tất cả đã sửa + có regression test, 2 lỗi rủi ro cao nhất có mutation-test): LLM client không còn crash mơ hồ khi provider trả `content: null` hoặc URL cấu hình sai (`LLM_BASE_URL`); field `reason` từ LLM không phải string không còn làm crash `HypothesisResult`/`ActionPlanResult`. Cơ chế chống fabricate host (Exploit Agent) đổi từ so khớp chuỗi con sang regex có ranh giới token thật (loại cả `\w`, `.`, và `-` — vì hostname thật hay có gạch nối) để không bị bypass bằng cách nhúng host giả vào giữa 1 chuỗi/tên miền không liên quan. `strip_markdown_json_fence` (dùng chung Hypothesis Engine + Exploit Agent) đổi từ "luôn lấy fence ĐẦU" (bug gốc) → "lấy fence CUỐI hợp lệ JSON" (vẫn có bug, chỉ đổi chiều) → cuối cùng nhận thêm `expected_keys` để chọn đúng fence có field tên đúng như engine gọi nó mong đợi, không đoán theo vị trí nữa. `Authorization` không còn nhận datetime thiếu timezone. `ActionPlan` không cho tạo plan rỗng (0 action) — trước đó sẽ được "approve" một cách vô nghĩa. `cli.py` đọc `--source` giờ bắt đủ lỗi file (không tìm thấy/là thư mục/không đọc được/không phải UTF-8) và ép `encoding="utf-8"` tường minh thay vì phụ thuộc locale hệ điều hành.

**Verification Package** (`shared/models/verification_package.py` + `verification_package/assembler.py`, SPEC §7, 2026-08-17) đã có: `VerificationPackage` đủ 19 trường (đánh số khớp bảng SPEC), `assemble_verification_package()` build từ artifact thật của 1 lượt chạy — tự gọi `decide()` trên đúng 1 danh sách observation dùng chung cho mọi trường khác (không nhận verdict tính sẵn riêng, tránh 2 nguồn sự thật lệch nhau), `action_record` chỉ giữ action thực sự tạo ra observation (không phải toàn bộ plan). `missing_fields_for_release()`/`is_release_ready` implement đúng "binary schema completeness" của §8.1 — khác ECS (rubric riêng, còn Proposed/TBD). Qua 1 vòng review ngay sau khi build, tìm ra 7 lỗi thật: `verdict`/`predicate_results` lưu tách rời không đồng bộ với nhau (validator của `VerdictResult` không tự áp dụng sang model này); `raw_evidence_references`/`artifact_hashes` chỉ kiểm độ dài, có thể bị hoán đổi vị trí cho nhau; `action_record` có thể thiếu ActionSpec cho action đã thực sự chạy, hoặc trùng lặp action_id; assembler crash khó hiểu với danh sách observation rỗng; `is_release_ready` không kiểm nội dung thật của human review (reviewer từ chối vẫn báo "sẵn sàng release"); lý do thật của verdict (`VerdictResult.reason`) bị bỏ qua, không có trong package. Cả 7 đã sửa + có test + 3 lỗi rủi ro cao nhất có mutation-test (mutation-test còn phát hiện 2 lỗi trong chính test tự viết — fixture bị trùng lặp khiến test "pass" vì lý do sai).

**Khoảng trống IDOR/BOLA đã đóng một phần bằng rule Semgrep tuỳ chỉnh, không phải bằng bypass thủ công** (`scanner_rules/semgrep/`, 2026-08-18): kiểm thử thật (Docker, NodeGoat + VAmPI, xem lịch sử dự án) xác nhận `--config auto` không có rule nào bắt được lớp lỗi Broken Object Level Authorization/IDOR (CWE-639) — không phải do chọn mẫu tín hiệu thiếu, mà do bản chất 3 tool (Semgrep pattern tĩnh, Trivy chỉ so CVE, ZAP baseline không có auth/multi-identity) không tool nào theo dõi được "định danh lấy từ URL có đối chiếu với session đang đăng nhập hay không". Thay vì tiếp tục viết ActionSpec tay bỏ qua Hypothesis Engine/Exploit Agent (vi phạm đúng nguyên tắc tín hiệu-trước-giả thuyết của kiến trúc), đã viết 2 rule Semgrep tuỳ chỉnh nhắm đúng idiom này (`idor_identity_from_url_param.yaml` cho Express, `bola_missing_auth_check.yaml` cho Flask) — chạy thật bằng Semgrep trên đúng file nguồn thật đã dùng để tìm lỗi thủ công trước đây (`allocations.js` của NodeGoat, `users.py` của VAmPI): cả 2 rule đều 1/1 đúng vị trí lỗi thật. Output thật đó đi thẳng qua `SemgrepAdapter`/`normalize`/`hypothesize` hiện có — **không sửa 1 dòng code adapter nào** — và sinh ra `Hypothesis` đầy đủ, có `observation_criteria` kiểm chứng được, hoàn toàn từ signal, không có ActionSpec viết tay nào nữa.

Review độc lập ngay sau đó (cùng ngày) tìm ra 5 lỗi thật trong bản rule đầu tiên: 2 false positive (rule JS không nhận diện `req.session` khi nó nằm trong điều kiện `if`, hoặc khi handler không có tên/ẩn danh — idiom phổ biến nhất của Express); 2 false negative (bracket notation `req.params['userId']` không bị bắt; rule Python bỏ sót hàm/method có nhiều hơn 1 tham số); và 1 chỗ tài liệu phóng đại (khẳng định "im lặng đúng" như tính chất tổng quát trong khi chỉ đúng với 1 mẫu đã thử). Cả 5 đã sửa, xác nhận lại bằng Semgrep thật, mỗi lỗi có 1 regression test riêng trong `tests/test_semgrep_adapter.py` (13 test cho phần này, tất cả mutation-verified). Chi tiết từng lỗi + giới hạn còn lại (rule theo idiom cụ thể chứ không phải bộ phát hiện IDOR tổng quát; vẫn bỏ sót `req['params'].userId`; `signal_context` vẫn bị Semgrep CLI miễn phí trả về `"requires login"` — dùng `--source` để bù) xem `scanner_rules/README.md`.

Chưa có: redaction policy đầy đủ theo danh mục thật (§4.3.5, cần chốt cùng owner ở Gate 3 — hiện chỉ có floor tối thiểu), manifest cấp package (§4.3.3). `execute` giờ ĐÃ tự dựng được kịch bản 3-role (main/positive_control/denied_control) đầy đủ, kể cả resource-ID-chaining động giữa các action — role tagging + multi-identity + blind marker seeding + resource-ID-chaining, xem 4 mục cùng ngày 2026-08-19 bên dưới.

**Audit toàn dự án 2026-08-18** (4 review độc lập song song, mỗi cái phụ trách 1 phần khác nhau của ~5600 dòng source — shared/models+Policy/Cost/Kill-Switch, Hypothesis Engine+Signal Normalizer+Context Store, Exploit Agent+Verdict Oracle+Verification Package, CLI+Evidence Harness): tìm ra 6 lỗi thật, tất cả đã sửa + có regression test + mutation-verified, xác nhận lại bằng 1 review độc lập thứ 5 riêng cho các bản fix.
1. **HIGH — path traversal qua percent-encoding trong Policy Service** (`shared/policy.py`): `GET .../api/objects/%2e%2e%2fadmin` khớp allowlist `.../api/objects/{id}` vì `{id}` chỉ kiểm path RAW (chưa decode) — cùng dạng scope-escape với lỗi host-bypass đã fix trước đó, chỉ khác ở path thay vì host. Sửa bằng decode + từ chối slash phát sinh hoặc segment `.`/`..`. Review độc lập thứ 2 (soi lại chính bản fix) tìm thêm 2 bypass: decode 1 lần không bắt được double-encoding (`%252e%252e%252f`); segment `..%5c` (backslash) không bị coi là traversal dù target kiểu Windows/IIS coi `\` là path separator. Sửa bằng decode lặp tối đa 5 vòng tới khi ổn định + từ chối luôn nếu path chứa `\`.
2. **HIGH — `VerdictResult`/`VerificationPackage` có thể tuyên bố CONFIRMED dù thiếu hẳn 1 nhóm predicate** (`shared/models/observation.py`, `shared/models/verification_package.py`): check cũ `all(satisfied)` đúng một cách vô nghĩa (vacuously true) với list chỉ có 1 nhóm (main) — không hề có positive_control/denied_control. Đúng lỗi SPEC gọi là "không có ngoại lệ". Sửa bằng check thêm: đủ đúng 3 nhóm bắt buộc, không thiếu không trùng.
3. **MEDIUM — `HypothesisResult` thiếu chiều validator ngược** (`shared/models/hypothesis.py`): `status=not_verifiable` kèm theo 1 `hypothesis` thật vẫn construct được — cùng dạng lỗi đã sửa ở `ActionPlanResult`/`PlanCheckResult`/`CostDecision`/`PlanReviewResult` trước đó, sót lại ở model này.
4. **MEDIUM — `CostService.cap` không bền vững qua các lần mở lại cùng `execution_id`** (`shared/cost.py`): trước đây chỉ đếm lại số hành động đã chạy, không lưu/đối chiếu lại `cap` — mở lại execution với `--cap` khác sẽ âm thầm đổi ngân sách đã duyệt. Sửa bằng ghi `cap` vào mỗi dòng audit log, từ chối khởi tạo nếu `cap` mới khác `cap` đã ghi nhận trước đó. Cũng bổ sung disclosure còn thiếu trong docstring: `CostService` (giống `KillSwitch`) không có cross-process lock thật — 2 instance cùng lúc vẫn có thể cùng vượt cap (đã biết, chưa sửa bằng lock thật, chỉ ghi rõ giới hạn).
5. **HIGH — `cmd_execute` không truyền `sensitive_body_keys` cho Evidence Harness** (`cli.py`): field nhạy cảm trong `ActionSpec.parameters` (password, API key — được Policy Service allowlist cho phép hợp lệ) bị ghi ra transcript bằng chứng ở dạng plaintext, vĩnh viễn. Thêm cờ CLI `--sensitive-param` (lặp lại được) nối vào `harness.capture()`.
6. **Gap chức năng — observation có cấu trúc không được lưu lại sau `execute`** (`cli.py`): `capture()` trả về `NormalizedObservation` chỉ dùng trong bộ nhớ cho `decide()`, không bao giờ ghi ra đĩa — trên đĩa chỉ có transcript thô thiếu `execution_id`/`target_id`/`channel`/hash/`access_result`. Không có đường nào (kể cả đọc lại file) để dựng lại observation cho Verification Package sau khi `execute` đã chạy xong. Thêm `observations.jsonl` (append-only, 1 dòng/observation, cùng thư mục evidence).

Không sửa (đã cân nhắc, quyết định giữ nguyên có lý do): anti-fabrication check của Exploit Agent bỏ qua hypothesis có sẵn DAST URL thật — đã có test xác nhận đây là chủ đích (Policy Service là lớp chặn thật sự cho trường hợp này). `verified_context`/Security Context Store write path lúc này vẫn chưa có (đúng như SPEC §4.6 mô tả — ghi qua Human Review, chưa hiện thực ở increment này) — xem phần "Security Context Store — write path" bên dưới cho khi việc này được hoàn thiện. 488/488 test pass (đã qua 2 vòng review độc lập cho cùng batch fix — vòng 2 soi lại chính các bản fix của vòng 1, tìm thêm đúng 1 lỗi thật ở mục 1).

**`secweave assemble-package` — CLI cho Verification Package** (2026-08-18, đóng 1 trong 3 gap còn lại ở trên): `assemble_verification_package()` giờ gọi được qua CLI thật, không cần Python API — đọc lại `observations.jsonl` (đã có), cộng 2 artifact mới `execute` tự lưu (`actions.json`, `execution_status.json` — trước đây chỉ là biến cục bộ, không ai đọc lại được sau khi `execute` kết thúc). 4 field bắt buộc là phán đoán con người (`--scenario`/`--limitations`/`--next-action`/`--authorization-reference`) cố tình không có giá trị mặc định. Review độc lập ngay sau đó tìm 2 lỗi thật: `actions.json` bị **ghi đè** thay vì gộp lại — reuse `execution_id` qua nhiều lần `execute` với plan khác nhau (pattern đã hỗ trợ sẵn ở Kill-Switch/Cost Service) làm mất vĩnh viễn ActionSpec của lần chạy trước, khiến `assemble-package` luôn lỗi sau đó; `scenario` là field bắt buộc duy nhất thiếu `min_length=1` so với 3 field còn lại. Cả 2 đã sửa + có regression test + mutation-verified. 492/492 test pass.

**`secweave review-package` — Human Review Loop tối thiểu (Gate 4, SPEC §4.5)** (2026-08-18): gắn `HumanReviewRecord` (reviewer/decision/reason/`checked_raw_artifact`) vào 1 package đã lắp, in ra danh sách raw evidence reference để reviewer thực sự đối chiếu. Thêm 1 validator: `decision=release` bắt buộc `checked_raw_artifact=true` (defense-in-depth ở tầng `HumanReviewRecord`, không chỉ ở tầng package như trước). Review độc lập tìm 1 lỗi **HIGH thật**: package JSON bị sửa tay (fabricate `verdict`+`predicate_results` khớp nhau về nội bộ, nhưng `normalized_observations` vẫn là bằng chứng thật không hề ủng hộ kết luận đó) vẫn qua được mọi validator cũ — chúng chỉ kiểm package tự nhất quán, không đối chiếu lại `predicate_results` với `normalized_observations` thật. Sửa bằng cách tính lại `evaluate_predicates()` trên observation thật trong file, lệch với `predicate_results` đã khai là từ chối thẳng. Thêm 2 fix nhỏ hơn: `--retest-reference` giờ chỉ hợp lệ cùng `--decision retest`; cảnh báo rõ ràng khi ghi đè 1 human_review_record đã có (không chặn — re-review là workflow hợp lệ — nhưng không được im lặng mất dấu vết reviewer trước). Tất cả có regression test + mutation-verified. 500/500 test pass.

**Security Context Store — write path (SPEC §4.6) hoàn thiện** (2026-08-18): trước khi làm, đã nghiên cứu thật 5 công cụ/chuẩn mã nguồn mở lớn để đối chiếu thiết kế (không tự nghĩ ra): VEX/OpenVEX (CISA/OpenSSF — vocabulary `not_affected`/`affected`/`fixed`/`under_investigation` + justification cố định), OWASP Dependency-Track (tách riêng "state" khỏi "suppressed" — áp dụng trực tiếp cho `status` khác `stale` trong thiết kế này), SARIF suppressions + `partialFingerprints`, GitHub Code Scanning dismissal reasons, OWASP DefectDojo (risk acceptance **bắt buộc có ngày hết hạn** — đúng nguyên lý SPEC đã nêu "kết luận an toàn không có thời hạn"). Kết quả: schema `verified_observations` thêm `execution_id`, `status` (unverified/verified — 2 giai đoạn ghi đúng theo diagram SPEC), `valid_until` (mọi verified fact đều có hạn, không bao giờ vô thời hạn), `stale`/`stale_reason` (nguyên tắc đánh dấu cũ — mark RỘNG hơn, không hẹp hơn). `EvidenceHarness.capture()` giờ ghi unverified ngay khi capture (context_store optional, cùng convention với kill_switch/cost_service — best-effort, không làm hỏng capture() nếu Context Store lỗi). `review-package --decision release` promote sang verified. Lệnh CLI mới `secweave mark-stale`. `hypothesize` giờ đọc cả unverified context (kèm nhãn cảnh báo, tách riêng khỏi verified trong prompt) — đúng luồng nét đứt trong SPEC, không chỉ đọc verified. Review độc lập tìm 2 lỗi thật: **HIGH** — `action.target` (kèm nguyên query string) bị ghi thẳng vào description, làm lộ blind marker/credential nhúng trong query value (đúng loại dữ liệu SPEC cấm tuyệt đối lưu) — sửa bằng cắt bỏ query string vô điều kiện trước khi ghi, không phụ thuộc `sensitive_body_keys` khai báo; `promote_execution_to_verified` không kiểm biên `valid_for_days` — giá trị ≤0 làm mất vĩnh viễn 1 observation khỏi verified, giá trị cực lớn crash bằng `OverflowError` không bắt được — sửa bằng ràng buộc 1..3650 ngày. Cả 2 đã sửa + regression test + mutation-verified. 523/523 test pass.

**Audit hygiene + kiến trúc toàn dự án** (2026-08-18): 4 review độc lập song song, lần này không tìm bug bảo mật (đã làm nhiều lần) mà tìm code chết, comment thừa/lặp, code trùng lặp, và đánh giá kiến trúc. Tự tay xác nhận + xoá 3 file/hàm chết thật: `shared/logging.py`, `shared/config.py` (rỗng hoàn toàn từ commit đầu tiên, chưa từng dùng), `shared/identity.py::get_execution_identity()` (tự docstring đã thừa nhận "hiện không dùng ở production"). Review tìm thêm **1 lỗ hổng bảo mật thật**: credential dạng HTTP Basic Auth nhúng thẳng trong URL (`https://admin:secret@host/...`) không bị redact ở cả artifact thô lẫn Context Store — cả 2 cơ chế redact trước đó chỉ xử lý query string, bỏ sót userinfo. Sửa bằng hàm `_strip_userinfo()` dùng chung cho cả 2 nơi. Dọn ~15 chỗ comment thừa/lặp lại chính nó qua nhiều vòng review trước (giữ 1 bản đầy đủ, các bản còn lại trỏ về thay vì kể lại toàn bộ câu chuyện) và trích xuất 4 helper dùng chung trong `cli.py` (`_open_context_store` gộp 7 chỗ giống hệt nhau, `_add_format_arg` gộp 6 chỗ, `_load_hypothesis_from_context_store` và `_build_local_test_authorization` gộp 2 chỗ mỗi cái giữa `plan`/`execute`).

**Đánh giá kiến trúc:** `cli.py` (~1450 dòng, 10 subcommand) có vấn đề thật — mỗi `cmd_*` vừa đọc argparse, vừa điều phối nghiệp vụ, vừa in kết quả trong cùng 1 hàm (`cmd_execute` một mình ~260 dòng), khiến 80 lời gọi test trong `tests/test_cli.py` không có cách nào kiểm logic nghiệp vụ mà không đi qua argparse + `capsys`. Khuyến nghị (chưa làm, quy mô lớn hơn 1 lượt cải tiến): tách phần điều phối ra hàm riêng nhận tham số đã parse, trả object kết quả thuần — chưa cần tách `cli.py` thành nhiều file ở quy mô hiện tại. `EvidenceHarness` (~950 dòng) KHÔNG phải God-class cần mổ xẻ toàn bộ — pipeline `capture()` là 1 luồng tuần tự thật sự cần nhiều state cục bộ; chỉ có phần quản lý session/client theo từng identity là đủ tách biệt để tách ra `IdentitySessionManager` riêng nếu muốn (không bắt buộc). Signal Normalizer (3 adapter), `predicates.py`/`oracle.py` (tách theo đúng "tính từng nhóm" vs "gộp thành verdict") vẫn nhất quán, không bị trôi qua nhiều lần sửa. 521/521 test pass sau toàn bộ đợt dọn dẹp.

**Refactor `cli.py` theo khuyến nghị ở trên (2026-08-19)**: thêm `CliError(Exception)` — mọi hàm nghiệp vụ (`_open_context_store`, `_load_signals`, `_build_llm_client`, `_load_hypothesis_from_context_store`, `_load_frozen_plan`, và logic bên trong mỗi `cmd_*`) giờ `raise CliError(message)` thay vì tự `print(...)` + `return 1`/`None`. Cả 10 `cmd_*` giờ chỉ còn 1 điểm bắt lỗi duy nhất ở boundary (`except CliError as exc: print(f"error: {exc}"); return 1`); 7/10 lệnh (hypothesize, show-hypothesis, plan, execute, assemble-package, review-package, kill, resume) tách thành `cmd_X`/`_run_X` để logic gọi được trực tiếp, không cần qua argparse. Message text giữ nguyên y hệt bản cũ (đã verify từng chỗ). Đây là refactor phần lỗi/luồng điều khiển, CHƯA làm tới mức "trả object kết quả thuần" như khuyến nghị gốc nói — nhánh thành công vẫn `print()` trực tiếp trong `_run_X`, nên `tests/test_cli.py` vẫn cần `capsys` để kiểm output thành công, chỉ riêng phần thông báo lỗi là đã thống nhất.

Review độc lập ngay sau đó (audit lại toàn dự án 1 lượt nữa, không chỉ riêng refactor) tìm thêm vài lỗi thật: `_run_execute` (nhánh không có `--plan-file`) tự chép lại y hệt logic của `_load_hypothesis_from_context_store` thay vì gọi hàm dùng chung — trái với chính docstring của hàm đó ("shared by plan and execute") — đã sửa bằng gọi lại hàm chung; 4 chỗ parse enum từ CLI flag (`--environment`/`--decision`/`--source`/`--automatic-threshold-reason`) lặp y hệt cùng 1 khuôn `try/except ValueError` — gộp thành `_parse_enum_arg()` dùng chung. Nghiêm trọng hơn: **`verification_package/assembler.py` chưa từng kiểm observations có cùng 1 `execution_id`** — `assemble_verification_package()` nhận `execution_id` là tham số tách rời, nhưng không đối chiếu với `execution_id` riêng của từng observation, nên 1 lời gọi vô tình gộp bằng chứng từ 2 lượt chạy khác nhau (vd `positive_control` từ run A, `denied_control` từ run B) vẫn lắp package "tự nhất quán" bình thường — đã sửa bằng cách từ chối thẳng nếu có observation lệch execution_id. Cả 3 đã sửa + có regression test, 522/522 test pass.

**3-role scenario — phần "role tagging" (2026-08-19):** đóng 1 trong 3 gap con của "`execute` chưa tự dựng được kịch bản 3-role" (2 gap còn lại — multi-identity trong CLI, tự động seed blind marker — cố tình để lại lượt sau, xem `cmd_execute`'s docstring). `ActionSpec` thêm field `role: ObservationRole` (mặc định `main`, tương thích ngược hoàn toàn với mọi plan cũ). Prompt của Exploit Agent dạy LLM: khi `suspected_behavior` thuộc dạng ranh giới truy cập (broken access control/IDOR/BOLA và tương tự) VÀ có thể thiết kế được action riêng cho từng vai trò, hãy gắn `role` (`main`/`positive_control`/`denied_control`/`setup`) cho từng action; các hypothesis khác vẫn để mặc định `main` như trước — không bắt buộc luôn dùng đủ 3 role. `_run_execute` giờ đọc `action.role` khi gọi `harness.capture()` thay vì hardcode `role=main` cho mọi action. Một giá trị `role` không hợp lệ từ LLM tự nhiên rơi vào validator Enum sẵn có của `ActionSpec` (không cần code riêng), surfaces thành `NOT_PLANNABLE` như mọi lỗi schema khác từ LLM. 532/532 test pass.

**3-role scenario — phần "multi-identity" (2026-08-19, tiếp):** đóng gap con thứ 2/3. `execute` thêm `--role-identity ROLE=LABEL` (lặp lại được — ánh xạ 1 role sang 1 identity LABEL khác `--identity` mặc định) và `--identity-logins <file JSON>` (khai method/target/parameters/token_json_path thật cho từng LABEL — chính là serialize lại tham số của `EvidenceHarness.login()` đã có sẵn, không thêm logic target-specific nào mới). Trước khi plan chạy, mọi LABEL được `--identity`/`--role-identity` tham chiếu tới VÀ có entry trong `--identity-logins` sẽ được `harness.login()` thật 1 lần; LABEL không có entry vẫn chạy được, chỉ là không đăng nhập (client mới, danh tính ẩn danh hợp lệ — vd denied_control chưa từng đăng nhập). Exploit Agent/LLM vẫn không đụng tới credential thật ở bước nào — plan chỉ mang `role`, LABEL/credential hoàn toàn do operator cấp qua CLI (đúng SPEC §4.2: "không tự lấy credential"). Login lỗi vì `token_json_path` sai (hoặc token rỗng/không phải string) là lỗi cấu hình → `CliError` sạch, không phải traceback; login bị kill-switch/cost-cap chặn giữa chừng thì xử lý y hệt 1 action bình thường trong vòng lặp chính (đã qua `capture()` nội bộ). Observation của mỗi login (role=setup) cũng được persist vào `observations.jsonl` như mọi observation khác, để `assemble-package` sau này vẫn dựng lại được đủ action record. 7 test mới (bao gồm 1 test đầy đủ: 2 identity đăng nhập bearer-token-trong-body thật — cùng dạng OWASP Juice Shop — routing đúng theo role, ra đúng access_result granted/denied cho từng identity). Review độc lập ngay sau đó tìm 1 lỗi thật: `identities_needing_login` lọc theo "được `--role-identity` tham chiếu" chứ chưa lọc theo "role đó có THẬT SỰ xuất hiện trong plan đang chạy không" — 1 `--role-identity` cho role plan không dùng tới vẫn kích hoạt login thật (tốn cost-cap, side effect thừa) cho 1 identity không action nào cần tới. Sửa bằng cách giao với `{action.role for action in plan_result.plan.actions}` trước khi quyết định login ai. Đã có regression test, 540/540 test pass.

**3-role scenario — phần "blind marker seeding" (2026-08-19, tiếp):** đóng gap con thứ 3/3, hoàn tất "`execute` chưa tự dựng được kịch bản 3-role". Trước đây `EvidenceHarness.generate_marker()` đã có nhưng không nơi nào gọi nó, và `capture(marker=...)` luôn nhận `None` — nghĩa là predicate `main` LUÔN `insufficient_data`, không bao giờ thật sự `satisfied` được qua CLI. Cơ chế: thêm hằng số dùng chung `BLIND_MARKER_PLACEHOLDER` (`shared/models/observation.py`) — 1 chuỗi cố định, KHÔNG bí mật. Prompt của Exploit Agent dạy LLM: khi dùng kịch bản 3-role có seed dữ liệu mồi an toàn, tạo 1 action `role=setup` có đúng 1 giá trị `parameters` là placeholder này (không phải giá trị thật). `_run_execute` phát hiện placeholder trong plan, gọi `generate_marker()` (qua 1 `EvidenceHarness` tạm — không mở client nào, chỉ để lấy marker, idempotent theo `execution_id` nên khớp với harness thật dựng sau đó) lấy marker THẬT, rồi thay placeholder bằng marker thật trong TOÀN BỘ action's parameters — làm việc này TRƯỚC `review_plan()`, không phải sau, vì allowlist `params:key=regex` phải kiểm đúng giá trị THẬT sẽ gửi (dạng 32 ký tự hex), không phải placeholder. Exploit Agent/LLM không bao giờ thấy giá trị marker thật — chỉ biết đúng 1 placeholder công khai, cố định (đúng SPEC §4.3.4: "Exploit Agent / mọi LLM: Không" biết marker). `actions.json` lưu lại giá trị THẬT (không phải placeholder), để `assemble-package` sau này dựng đúng action record. Cập nhật 1 test cũ (`test_build_plan_prompt_never_contains_blind_marker_concept` → đổi tên + nới điều kiện: prompt được phép nhắc tới placeholder cố định, chỉ cấm 1 giá trị marker THẬT — vốn không thể xuất hiện ở bước build prompt vì `generate_marker()` chỉ chạy sau, trong `execute`). 2 test mới (1 test đầy đủ: seed thật → đọc lại → `main` SATISFIED; 1 test xác nhận plan không dùng placeholder thì hành vi y hệt trước đây — không tự sinh `seed_manifest.json`, marker fields vẫn `None`).

Review độc lập ngay sau đó (đặc biệt kỹ vì đây là cơ chế nhạy cảm bảo mật nhất trong đợt này — có thật sự đảm bảo LLM không bao giờ thấy giá trị marker thật không) xác nhận 5/6 điểm kiểm tra sạch, tìm ra đúng 1 lỗi thật: bản thay thế placeholder ban đầu chỉ quét giá trị string ở CẤP NGOÀI CÙNG của `action.parameters` — LLM (đã ghi nhận có lúc không tuân lệnh đúng) đặt placeholder vào `action.target`/`description`, hoặc lồng trong 1 list/dict bên trong `parameters` (vd mảng `tags`), sẽ lọt qua mà KHÔNG báo lỗi gì — target thật âm thầm nhận đúng chuỗi placeholder thay vì marker thật, không ai biết trừ khi đọc tay transcript. Sửa bằng cách quét/thay thế đệ quy trên toàn bộ `target`/`description`/`method`/`parameters` (mọi cấu trúc dict/list JSON có thể có), cộng 1 kiểm tra phòng vệ sau khi thay: còn sót placeholder ở đâu thì `raise CliError` ngay (thất bại to tiếng thay vì âm thầm sai) — dù về lý thuyết không nên xảy ra nữa sau khi quét đệ quy đầy đủ. Đã có regression test riêng cho trường hợp lồng trong list. 543/543 test pass.

**Chạy thử thật toàn bộ pipeline lần đầu (2026-08-19)** — normalize → hypothesize (LLM thật) → plan (LLM thật) → execute (request thật tới 1 local demo server tự dựng, có IDOR thật) → assemble-package → review-package, trên 1 kịch bản 3-role/multi-identity/blind-marker đầy đủ — ra đúng verdict `CONFIRMED`, release được. Chạy thật (không phải chỉ test mock) lộ ra 2 lỗi thật mà không test nào trong 544 test hiện có bắt được, vì chưa test nào từng nối `execute` (dùng `--identity-logins`) với `assemble-package` trong cùng 1 kịch bản:
1. **`actions.json` không có ActionSpec cho action do `harness.login()` tạo** — `_persist_actions()` (trước đây chỉ chạy 1 lần, trước cả vòng lặp login) chỉ gộp `plan_result.plan.actions`, không bao giờ thấy được `ActionSpec` login tự dựng bên trong vòng lặp login — `assemble-package` sau đó từ chối cả package với "action_record thiếu ActionSpec cho action_ref" ngay khi có dùng `--identity-logins`. Sửa bằng cách tách `_persist_actions()` thành hàm dùng lại được, gọi thêm 1 lần cho từng login action ngay khi nó được tạo (trước cả khi thử login, để vẫn ghi lại được ngay cả khi login thất bại).
2. **`ActionSpec` của login mang `role=main` (giá trị mặc định) dù observation thật của nó luôn là `role=setup`** — `harness.login()` tự gán `role=SETUP` cho observation, nhưng `ActionSpec` truyền vào không được gán `role` tường minh nên lấy mặc định `main`, khiến `action_record` nói dối về role của chính action đó. Sửa bằng gán `role=ObservationRole.SETUP` tường minh khi dựng login action.

Cả 2 đã sửa + có regression test (`test_cli_assemble_package_works_after_a_multi_identity_execute_run`), 544/544 test pass. Đây là bằng chứng cụ thể cho lý do dự án luôn ưu tiên ít nhất 1 lần chạy thật end-to-end bên cạnh test mock — 2 lỗi này chỉ lộ ra khi 2 tính năng (multi-identity login + assemble-package) được dùng CÙNG NHAU trong 1 kịch bản thật, điều chưa test nào trước đó làm.

**Resource-ID-chaining (2026-08-19, tiếp) — đóng gap thật vừa thấy ở lần chạy trên:** lần chạy thật đó, LLM tự chọn đúng thiết kế 3-role nhưng để lại placeholder chưa resolve (`{note_id_A}`, tự bịa `path_params`) vì không có cách nào diễn đạt "action sau cần dùng ID do server cấp ở action trước" — phải viết `--plan-file` tay mới chạy tiếp được. Đóng bằng cú pháp `{{FROM_STEP:<step_id>:<json_path>}}`: `ActionSpec` thêm field `step_id` (tuỳ chọn, mặc định `None`) để đánh dấu 1 action mà response thật của nó có thể bị tham chiếu sau; 1 action ĐỨNG SAU dùng cú pháp trên trong `target`/`parameters` để tham chiếu — Exploit Agent's prompt dạy LLM đúng quy ước này, không tự đoán/bịa ID cụ thể nữa. Khác blind marker (resolve 1 lần trước `review_plan()`), FROM_STEP bắt buộc resolve XEN KẼ trong vòng lặp capture chính — giá trị thật chỉ tồn tại sau khi action nguồn đã chạy — nhưng Policy Service vẫn kiểm được plan CHƯA resolve (cú pháp `{param}` trong allowlist vốn khớp bất kỳ text nào ở 1 path segment, kể cả text placeholder). `parameters` giữ đúng KIỂU DỮ LIỆU gốc khi giá trị khớp TOÀN BỘ 1 placeholder (số thật vẫn là số, không bị ép thành chuỗi); nhúng trong chuỗi lớn hơn thì stringify. `_persist_actions()` đổi sang "action_id trùng thì bản mới thắng" để `actions.json` cuối cùng giữ giá trị THẬT đã resolve, không phải placeholder. Tham chiếu step_id chưa chạy/không tồn tại → `CliError` sạch. Đã chạy lại thật đúng kịch bản đã fail trước đó (server local tự dựng) — không còn cần hardcode ID tay, ra đúng verdict `CONFIRMED` như khi hardcode. 5 test mới, 549/549 test pass.

Review độc lập ngay sau đó (đặc biệt kỹ điểm: Policy Service kiểm plan CHƯA resolve có an toàn không) tìm ra 3 lỗi thật, 1 trong đó **HIGH — bypass allowlist thật**:
1. **HIGH**: Policy Service chỉ kiểm plan CHƯA resolve ở `review_plan()`, KHÔNG BAO GIỜ kiểm lại giá trị ĐÃ resolve trước khi gửi request thật. Vì `{{FROM_STEP:...}}` không chứa `/` nên luôn khớp `{id}` kiểu wildcard bất kể giá trị thật sau này là gì — nếu response thật (dữ liệu runtime, đúng loại dữ liệu 1 kịch bản IDOR phải nghi ngờ) trả về 1 giá trị dạng `"42/../../admin/settings"`, request thật sẽ vượt hẳn phạm vi allowlist đã duyệt mà không ai kiểm lại. Sửa bằng cách gọi lại `is_allowed()` trên đúng action ĐÃ resolve, ngay trước khi gửi request thật — từ chối nếu không còn khớp, đúng nguyên tắc deny-by-default áp dụng cho THỨ THỰC SỰ được gửi, không chỉ bản thiết kế trên giấy.
2. `step_id` bị 2 action khác nhau dùng trùng — response của action sau âm thầm ghi đè action trước trong `step_responses`, FROM_STEP tham chiếu sau đó không rõ đang trỏ tới action nào. Sửa bằng từ chối thẳng (`CliError`) nếu phát hiện trùng.
3. Giá trị resolve được là 1 object/list (không phải giá trị đơn) nhưng bị nhét thẳng vào `target`/`description` (dạng string) bằng `str()` — tạo ra URL vô nghĩa (dù `harness.capture()` đã bắt được `httpx.InvalidURL` từ trước, không crash, chỉ lãng phí 1 slot cost cho request vô nghĩa). Sửa bằng từ chối thẳng khi giá trị resolve không phải kiểu đơn.

Cả 3 đã sửa + có regression test riêng cho từng cái (bao gồm 1 test tái hiện chính xác kịch bản path-traversal ở mục 1), xác nhận lại bằng chạy thật lần nữa (cùng kịch bản CONFIRMED ở trên vẫn đúng sau khi thêm re-check). 552/552 test pass.

**`secweave retest` — reproducibility tự động (SPEC §8.1, WEEKLY_PLAN W7, 2026-08-19)**: trước đây đo tỷ lệ cùng verdict qua ≥3 lần chạy lại hoàn toàn thủ công (operator tự chạy `execute` nhiều lần, tự ghi `--retest-reference`). Lệnh mới `retest` (dùng chung hầu hết flag với `execute`, tách qua `_add_execute_common_args()`) tự chạy lại ĐÚNG 1 `--plan-file` (bắt buộc, khác `execute` — LLM tự lập lại plan mỗi lần sẽ lẫn "LLM không tất định" với "hệ thống không lặp lại được", 2 câu hỏi khác nhau) `--runs` lần (mặc định 3), mỗi lần 1 `execution_id` riêng (không dùng chung kill-switch/cost — đo đúng khả năng lặp lại, không cộng dồn ngân sách). In ra TOÀN BỘ verdict từng lần, không có đường nào để chỉ báo cáo lần "đẹp nhất" (WEEKLY_PLAN W7 gọi đây là hành vi bị cấm, tương đương gian lận bằng chứng). Lưu 1 file tóm tắt JSON (`retest_id`, tỷ lệ cùng verdict, có đạt ngưỡng đề xuất ≥2/3 không) — `retest_id` là thứ nên truyền cho `review-package --retest-reference` sau đó. Đã chạy thật 3 lần độc lập trên server demo — ra đúng CONFIRMED cả 3/3.

Review độc lập tìm 2 vấn đề thật: (1) `agreement_ratio` một mình không phân biệt được "verdict thật sự khác nhau" với "1 lần bị dừng giữa chừng vì kill-switch/cost-cap không liên quan" — cả 2 trông giống hệt nhau ở field đó — sửa bằng thêm field `runs_with_no_verdict` + cảnh báo rõ trong output; (2) test đo "disagreement" ban đầu vô tình đặt verdict thiểu số ở vị trí TRÙNG với verdict đa số của lần đầu tiên, nên 1 bản cài sai (lấy kết quả lần đầu thay vì tính đa số thật) vẫn qua được test — sửa bằng đổi thứ tự fixture để lần 1 là thiểu số. Bản sửa (1) tự lộ thêm 1 bug thật khi viết test cho chính nó: nếu CẢ 3 lần đều bị BLOCKED trước khi `EvidenceHarness` từng được tạo, `--storage-dir` chưa từng tồn tại, ghi file summary sẽ crash — sửa bằng `mkdir(parents=True, exist_ok=True)` trước khi ghi. 8 test mới, 558/558 test pass.

## Cài đặt

Yêu cầu Python 3.10+.

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Copy `.env.example` thành `.env`, điền `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` — dùng được bất kỳ provider nào tương thích chuẩn OpenAI Chat Completions (đã thử: Groq, Google Gemini). `.env` bị `.gitignore` chặn, không bao giờ commit được.

`tests/test_end_to_end.py` (2026-08-17, sau khi xác nhận trước đó không có test nào nối các tầng lại với nhau) nối thật Signal Normalizer → Hypothesis Engine → Exploit Agent → Evidence Harness → Verdict Oracle trong 1 test (chỉ mock network + LLM, còn lại là logic thật 100%) — verdict cuối ra đúng `CONFIRMED` cho 1 kịch bản IDOR 3 role, cộng 1 test cho nhánh `NOT_PLANNABLE` (finding SAST thuần, không có network location). Đã mutation-test: cố tình phá đường truyền field `location` từ signal sang hypothesis, xác nhận test bắt được lỗi ngay.

## Chạy test

```bash
pytest
```

## Dùng CLI

11 lệnh, dùng report mẫu có sẵn trong `tests/fixtures/` để thử ngay.

Các file mẫu trong `tests/fixtures/` là output **thật** (không tự viết tay) từ Semgrep/Trivy/OWASP ZAP chạy thật vào SecWeave/OWASP Juice Shop — xem chú thích trong từng file. Ví dụ dưới dùng `zap_sample_report.json` vì nó có URL thật, đi được hết cả 4 bước; `semgrep_sample_report.json`/`trivy_sample_report.json` là whitebox (SAST/SCA, không có URL) nên `plan` sẽ trả `NOT_PLANNABLE` — đúng hành vi mong đợi, không phải lỗi (xem `--tool` bên dưới để thử các file đó).

**1. Chuẩn hoá report thô thành `NormalizedSignal`** (không cần API key):
```bash
python cli.py normalize --signal tests/fixtures/zap_sample_report.json --tool owasp_zap --tool-version 2.17.0
```
`--tool` nhận `semgrep` / `trivy` / `owasp_zap`. Thêm `--format json` để lấy output JSON.

**2. Sinh `Hypothesis` từ report** (gọi LLM thật, cần `.env`):
```bash
python cli.py hypothesize --signal tests/fixtures/zap_sample_report.json --tool owasp_zap --tool-version 2.17.0 --context-db .secweave/context.db
```
Không có API key thì thêm `--llm-mode agent` để bắc cầu qua agent đang chat cùng bạn thay vì gọi API.

**3. Soạn `ActionPlan` từ 1 hypothesis đã lưu, kiểm duyệt qua allowlist** (chưa thực thi request thật nào):
```bash
python cli.py plan --hypothesis-id <hyp_...> \
  --allowed-action "GET http://host.docker.internal:3000" \
  --context-db .secweave/context.db
```
`--allowed-action` lặp lại được nhiều lần; không truyền gì = allowlist rỗng = mọi hành động bị chặn (deny-by-default). LLM thật không tất định — có lúc chỉ đề xuất 1 action (khớp allowlist trên → `APPROVED`), có lúc đề xuất thêm action khác (vd `OPTIONS`, hay `GET` kèm tham số `Origin`) không có trong allowlist → `BLOCKED`. **Cả 2 kết quả đều đúng** — `BLOCKED` không phải lỗi, đó chính là deny-by-default hoạt động đúng khi allowlist không phủ hết plan. Muốn chắc `APPROVED`, thêm allowlist entry khớp với action thực tế LLM vừa đề xuất (đọc từ output ngay phía trên).

**4. Tra lại hypothesis đã sinh:**
```bash
python cli.py show-hypothesis --hypothesis-id <hyp_...>
```

**5. Thực thi plan đã duyệt, thu bằng chứng thật (`execute`)** — SẼ GỬI REQUEST THẬT, chỉ chạy khi thực sự được phép trên target đó:
```bash
python cli.py execute --hypothesis-id <hyp_...> \
  --allowed-action "GET http://host.docker.internal:3000" \
  --target-id tgt_juiceshop --target-revision-id rev_local_docker \
  --execution-id exec_demo_1 \
  --storage-dir .secweave/evidence --context-db .secweave/context.db
```
In ra verdict cuối (thường `inconclusive` cho finding 1-role như trên — thiếu positive/denied control, đúng hành vi, không phải lỗi). Ghi lại `--execution-id` (tự đặt hoặc đọc dòng `-> execution_id:` nếu không truyền) để dùng ở bước 7.

Kịch bản đủ 3-role (main/positive_control/denied_control, nhiều identity thật qua `--role-identity`/`--identity-logins`, blind marker qua `{{SECWEAVE_BLIND_MARKER}}`, ID tài nguyên động qua `{{FROM_STEP:step_id:json_path}}`) — xem `execute --help` và ví dụ đầy đủ tại `.secweave/manual_test/identity_scenario_example.py` (kịch bản IDOR thật đã tìm ra lỗi thật của Juice Shop).

**6. Đo tính lặp lại — chạy lại ĐÚNG 1 plan nhiều lần độc lập (`retest`, SPEC §8.1)** — tuỳ chọn, cần đóng băng plan trước:
```bash
python cli.py plan --hypothesis-id <hyp_...> \
  --allowed-action "GET http://host.docker.internal:3000" \
  --context-db .secweave/context.db --format json > plan.json

python cli.py retest --hypothesis-id <hyp_...> --plan-file plan.json \
  --allowed-action "GET http://host.docker.internal:3000" \
  --target-id tgt_juiceshop --target-revision-id rev_local_docker \
  --storage-dir .secweave/evidence --context-db .secweave/context.db --runs 3
```
`--plan-file` bắt buộc cho `retest` (khác `execute`, nơi nó tuỳ chọn) — để LLM tự lập lại plan mỗi lần sẽ lẫn "LLM không tất định" với "hệ thống không lặp lại được". In ra verdict của TỪNG lần, không có đường nào để chỉ báo cáo lần "đẹp nhất"; lưu 1 file tóm tắt JSON — `retest_id` bên trong dùng cho `--retest-reference` ở bước 8.

**7. Lắp Verification Package đủ 19 trường (`assemble-package`)**:
```bash
python cli.py assemble-package --execution-id exec_demo_1 \
  --storage-dir .secweave/evidence \
  --target-id tgt_juiceshop --target-revision-id rev_local_docker \
  --environment sandbox --authorization-reference auth_local_test_1 \
  --scenario "Cross-Domain Misconfiguration tại host.docker.internal:3000" \
  --limitations "Chỉ có role=main, thiếu positive/denied control." \
  --next-action "Không cần thêm — quan sát trực tiếp." \
  --format json > package.json
```

**8. Con người review, quyết định release (`review-package`, Gate 4)**:
```bash
python cli.py review-package --package-file package.json \
  --context-db .secweave/context.db \
  --reviewer "<tên người review>" --decision release \
  --reason "Đã đối chiếu raw evidence, khớp normalized observation." \
  --checked-raw-artifact
```
In ra danh sách raw evidence reference cần tự tay đối chiếu TRƯỚC khi chạy lệnh này — bắt buộc, không phải gợi ý. `decision=release` bắt buộc kèm `--checked-raw-artifact`; `--decision retest`/`reject` không cần. `release` thành công sẽ promote observation sang `verified` trong Context Store.

**9. Dừng khẩn 1 execution đang chạy, từ terminal KHÁC (`kill`, SPEC §6.3)**:
```bash
python cli.py kill --execution-id exec_demo_1 --storage-dir .secweave/evidence \
  --source operator --reason "Phát hiện hành vi ngoài dự kiến, dừng ngay để kiểm tra."
```
`--source` nhận 1 trong 6 nguồn SPEC §6.3 (`operator`/`target_owner`/`infra_owner`/`data_isms_owner`/`incident_responder`/`automatic_threshold` — nguồn cuối bắt buộc kèm `--automatic-threshold-reason`).

**10. Cho 1 execution đã STOPPED chạy lại (`resume`, đường DUY NHẤT)**:
```bash
python cli.py resume --execution-id exec_demo_1 --storage-dir .secweave/evidence \
  --authorization-reference "Owner đã duyệt lại qua email lúc 14:00 20/08/2026"
```

**11. Đánh dấu 1 verified fact trong Context Store là đã cũ (`mark-stale`, SPEC §4.6)**:
```bash
python cli.py mark-stale --target-id tgt_juiceshop \
  --reason "Target đã đổi revision, ngữ cảnh cũ có thể không còn đúng." \
  --context-db .secweave/context.db
```

Mỗi lệnh có `--help` riêng để xem đầy đủ tuỳ chọn.

**Phân loại:** NTQ INTERNAL.
